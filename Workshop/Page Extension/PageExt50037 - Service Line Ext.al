pageextension 50037 "Service Line Ext" extends "Service Invoice Subform"
{
    layout
    {
        addafter("VAT Prod. Posting Group")
        {
            field("Suggested Qty"; Rec."Suggested Qty") { ApplicationArea = All; }
            field("Status"; Rec."Status") { ApplicationArea = All; }
            field("Creation Date"; Rec."Creation Date") { ApplicationArea = All; }
            field(".x"; Rec.".x") { ApplicationArea = All; }
            field("Quantity Issued"; Rec."Quantity Issued") { ApplicationArea = All; }
            field("Inventory at Branch"; Rec."Inventory at Branch") { ApplicationArea = All; }
            field("..xx"; Rec."..xx") { ApplicationArea = All; }
            field("Unblock Usage"; Rec."Unblock Usage") { ApplicationArea = All; }
            field("Customer Job Type"; Rec."Customer Job Type") { ApplicationArea = All; }
            field("Job Type Code"; Rec."Job Type Code") { ApplicationArea = All; }
            field("Total Cost Amount"; Rec."Total Cost Amount") { ApplicationArea = All; }
            field("Allow Approved Usage"; Rec."Allow Approved Usage") { ApplicationArea = All; }
            field("User ID- BLocked Item Removed"; Rec."User ID- BLocked Item Removed") { ApplicationArea = All; }
            field("BLocking Notification"; Rec."BLocking Notification") { ApplicationArea = All; }
            field("Last Inv Doc"; Rec."Last Inv Doc") { ApplicationArea = All; }
            field("Last Inv Date"; Rec."Last Inv Date") { ApplicationArea = All; }
            field("Unblock-Approver"; Rec."Unblock-Approver") { ApplicationArea = All; }
            field("Cost Amount"; Rec."Cost Amount") { ApplicationArea = All; }
            field("Send To"; Rec."Send To") { ApplicationArea = All; }
            field("Sender"; Rec."Sender") { ApplicationArea = All; }
            field("Send For Approval"; Rec."Send For Approval") { ApplicationArea = All; }
            field("Sent Date"; Rec."Sent Date") { ApplicationArea = All; }
            field("Approve/Reject"; Rec."Approve/Reject") { ApplicationArea = All; }
            field("Approved By"; Rec."Approved By") { ApplicationArea = All; }
            field("Approval Date"; Rec."Approval Date") { ApplicationArea = All; }
            field("Reasons for Rejecting Part"; Rec."Reasons for Rejecting Part") { ApplicationArea = All; }
            field("Quantity CONSM Per Year"; Rec."Quantity CONSM Per Year") { ApplicationArea = All; }
            field("Reason For Approval"; Rec."Reason For Approval") { ApplicationArea = All; }
            field("Quantity Requesting For approv"; Rec."Quantity Requesting For approv") { ApplicationArea = All; }
            field("Invoiced Lines"; Rec."Invoiced Lines") { ApplicationArea = All; }
            field("Inventory Dynamic"; Rec."Inventory Dynamic") { ApplicationArea = All; }
            field("User ID"; Rec."User ID") { ApplicationArea = All; }
            field("Item Type"; Rec."Item Type") { ApplicationArea = All; }
            field("Header Status"; Rec."Header Status") { ApplicationArea = All; }
            field("Retails markup"; Rec."Retails markup") { ApplicationArea = All; }
            field("Return to Store"; Rec."Return to Store") { ApplicationArea = All; }
            field("DifferenceWIP"; Rec."DifferenceWIP") { ApplicationArea = All; }
            field("WIP Total Cost"; Rec."WIP Total Cost") { ApplicationArea = All; }
            field("39004253"; Rec."39004253") { ApplicationArea = All; }
            field("Posted Service Inv Qty."; Rec."Posted Service Inv Qty.") { ApplicationArea = All; }
            field("Code"; Rec."Code") { ApplicationArea = All; }


        }

    }

    actions
    {
    }
}